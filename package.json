{
  "name": "homehub",
  "version": "0.1.0",
  "description": "Self-hosted household management dashboard.",
  "private": true,
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/ThomasRooyakkers/HomeHub.git"
  },
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "npm --prefix frontend run build",
    "test": "npm run test:backend && npm run test:frontend",
    "test:backend": "npm --prefix backend test",
    "test:frontend": "TZ=America/New_York npm --prefix frontend test -- --runInBand",
    "test:smoke": "npm --prefix frontend run test:e2e"
  }
}
